## Analisis del problema

*Entradas
    Se define la función calcularInventario
    Se definen los parámetros de la función (inicial, vendidos y recibidos)

*Proceso
    Se valida que no haya valores negativos en las entradas por medio de una condiciona if
    Se retorna error en caso de que los valores sean negativos
    Se realiza cálculo matemático de resta y suma
    Se valida que el resultado no sea negativo en donde haya más ventas que stock
    Se evalúa que el invenatrioFinal no sea menor que 5 para saber si el inventario es estable o crítico

*Salida
    Mensaje de inventario final y su estado

## Justificación

*Se creo una función con sus parámetros y se utilizó el condicionante if y la sentencia return para finalizar la ejecución y devuelva un valor especifíco.